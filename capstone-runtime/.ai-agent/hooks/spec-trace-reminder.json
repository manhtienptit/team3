{
  "version": "v1",
  "hooks": [
    {
      "name": "Spec-trace Update Reminder",
      "trigger": "PostFileCreate",
      "description": "Reminds to update spec-trace.md when new test files are created",
      "matcher": "tests/.*\\\\.py$",
      "action": {
        "type": "agent",
        "prompt": "A new test file was created in tests/. Per project rules, every test must have a corresponding row in spec-trace.md. Check if the new test ids need to be added to spec-trace.md and add them if missing."
      }
    }
  ]
}